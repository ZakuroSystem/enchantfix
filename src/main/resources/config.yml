# 監査間隔（秒）
scan-interval-seconds: 30

# 置換ルール
rules:
  max-level: 10

# エンダーチェストも監査する場合 true
include-ender-chest: false

# ログファイル（plugins/EnchantAuditor/logs/<date>.log）
log:
  date-pattern: "yyyy-MM-dd"
  time-pattern: "HH:mm:ss"
